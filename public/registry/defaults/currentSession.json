{
  "name": "currentSession",
  "files": [
    {
      "name": "currentSession.ts",
      "content": "\"use server\"\n import { auth } from \"@/auth\";\n\"use client\";\nexport async function currentUserinSession() {\n  try {\n    const session = await auth();\n    if (session) {\n      return session.user;\n    }\n    return null;\n  } catch (error) {\n    console.log(error);\n  }\n}"
    }
  ],
  "type": "lib:utils"
}
